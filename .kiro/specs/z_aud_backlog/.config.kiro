{
  "spec_name": "z_aud_backlog",
  "title": "Backlog audit — résidus P1/mineurs : providers sociaux, backends SMS, bug bounty léger, DX résiduelle",
  "version": "1.0.0",
  "status": "draft",
  "source": "AUDIT.md §8 P1 (F8 providers, F10 SMS), §8 P0 parenthèse (bug bounty léger), §4.3 (DX résiduelle) — items relégués en backlog par z_aud_1..z_aud_5",
  "depends_on": ["z_aud_1"],
  "relates_to": ["z_aud_4", "z_aud_5"],
  "files": [
    "readme.md",
    "base.md",
    "requirements.md",
    "design.md",
    "tasks.md",
    "manual_tests.md"
  ],
  "language": "fr",
  "test_framework": "pytest + hypothesis",
  "min_property_examples": 100,
  "note": "Spec légère de complétion : ferme les 4 écarts résiduels identifiés par l'analyse de couverture AUDIT.md vs z_aud_1..5. Exécutable en parallèle de n'importe quelle phase après z_aud_1."
}
