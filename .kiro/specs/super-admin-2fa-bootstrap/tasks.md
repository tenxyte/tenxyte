# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Super Admin Bootstrap Token Issuance
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For deterministic bugs, scope the property to the concrete failing case(s) to ensure reproducibility
  - Test that when an admin user (is_superuser=True OR is_staff=True) with mfa_type="none" attempts to log in via LoginEmailView or LoginPhoneView, the system issues a restricted token with scope="2fa_setup_only", requires_2fa_setup=true, and token lifetime <= 900 seconds
  - Generate test cases covering:
    - Superuser created via createsuperuser (is_superuser=True, mfa_type="none")
    - Staff user without 2FA (is_staff=True, mfa_type="none")
    - Role elevation scenario (regular user elevated to admin without 2FA)
    - Both email and phone login paths
  - The test assertions should match the Expected Behavior Properties from design:
    - ASSERT result.success = true
    - ASSERT result.token_scope = "2fa_setup_only"
    - ASSERT result.requires_2fa_setup = true
    - ASSERT result.access_token IS NOT NULL
    - ASSERT result.token_lifetime <= 900
    - ASSERT can_call_endpoint(result.access_token, "/2fa/setup/") = true
    - ASSERT can_call_endpoint(result.access_token, "/2fa/confirm/") = true
    - ASSERT can_call_endpoint(result.access_token, "/2fa/status/") = false
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found:
    - 403 ADMIN_2FA_SETUP_REQUIRED error when admin without 2FA attempts login
    - No token issued, blocking access to 2FA setup endpoints
    - Circular dependency confirmed
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 2.1, 2.2, 2.5_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Existing Authentication Flows Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (cases where isBugCondition returns false)
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - Regular users (non-admin) with various 2FA states can log in
    - Admin users with 2FA enabled must provide valid TOTP code during login
    - Token refresh operations validate that admins have 2FA enabled
    - 2FA disable endpoint remains restricted for admins
    - Backup code authentication works for admins with 2FA enabled
    - Magic link and social authentication flows unchanged
    - Session limits, device fingerprinting, and security features unchanged
  - Property-based testing generates many test cases for stronger guarantees
  - Test cases to observe and capture:
    - Regular user login (non-admin, 2FA disabled) → full token issued
    - Regular user login (non-admin, 2FA enabled) → TOTP required, full token issued
    - Admin with 2FA enabled login → TOTP required, full token issued
    - Admin with 2FA enabled login with backup code → code marked used, full token issued
    - Token refresh for admin with 2FA → new access token issued
    - Token refresh for regular user → new access token issued
    - 2FA disable attempt by admin → 403 forbidden
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [x] 3. Fix for Super Admin 2FA Bootstrap Circular Dependency

  - [x] 3.1 Extend JWTService to support restricted token scopes
    - Modify `src/tenxyte/core/jwt_service.py`
    - Add optional parameter `custom_lifetime: Optional[timedelta] = None` to `generate_access_token()` method
    - Support "scope" claim in `extra_claims` parameter for restricting token access
    - Document scope claim usage in docstring
    - _Bug_Condition: isBugCondition(input) where (input.user.is_superuser = true OR input.user.is_staff = true) AND input.user.mfa_type = "none" AND input.attempting_login = true_
    - _Expected_Behavior: Issue token with scope="2fa_setup_only", lifetime=900 seconds, requires_2fa_setup=true_
    - _Preservation: Token generation for non-bootstrap cases unchanged_
    - _Requirements: 2.1, 2.5_

  - [x] 3.2 Update require_jwt decorator to validate token scope
    - Modify `src/tenxyte/decorators.py`
    - Extract "scope" claim from decoded JWT token payload
    - Store scope on request object (e.g., `request.jwt_scope`)
    - Add scope validation mechanism allowing views to declare required scopes
    - Provide decorator parameter or view attribute for scope enforcement
    - _Bug_Condition: Tokens with restricted scope must only access permitted endpoints_
    - _Expected_Behavior: Reject tokens with insufficient scope with 403 INSUFFICIENT_SCOPE_
    - _Preservation: Existing JWT validation logic unchanged for full-scope tokens_
    - _Requirements: 2.4_

  - [x] 3.3 Modify LoginEmailView to issue bootstrap tokens
    - Modify `src/tenxyte/views/auth_views.py` - LoginEmailView.post() at line 754
    - Replace 403 rejection at lines 796-800 with bootstrap token issuance
    - When admin lacks 2FA (is_superuser or is_staff, mfa_type="none"):
      - Call JWTService with extra_claims={"scope": "2fa_setup_only"} and custom_lifetime=timedelta(minutes=15)
      - Return 200 response with token, requires_2fa_setup=true flag
    - Preserve existing behavior for admin with 2FA enabled (still require TOTP)
    - _Bug_Condition: isBugCondition(input) for email login path_
    - _Expected_Behavior: expectedBehavior(result) - issue restricted token with 15-minute expiration_
    - _Preservation: Non-bootstrap login flows unchanged (admin with 2FA, non-admin users)_
    - _Requirements: 2.1, 2.2, 2.5, 3.1, 3.4_

  - [x] 3.4 Modify LoginPhoneView to issue bootstrap tokens
    - Modify `src/tenxyte/views/auth_views.py` - LoginPhoneView.post() at line 845
    - Replace 403 rejection at lines 962-966 with bootstrap token issuance
    - When admin lacks 2FA (is_superuser or is_staff, mfa_type="none"):
      - Call JWTService with extra_claims={"scope": "2fa_setup_only"} and custom_lifetime=timedelta(minutes=15)
      - Return 200 response with token, requires_2fa_setup=true flag
    - Preserve existing behavior for admin with 2FA enabled (still require TOTP)
    - _Bug_Condition: isBugCondition(input) for phone login path_
    - _Expected_Behavior: expectedBehavior(result) - issue restricted token with 15-minute expiration_
    - _Preservation: Non-bootstrap login flows unchanged (admin with 2FA, non-admin users)_
    - _Requirements: 2.1, 2.2, 2.5, 3.1, 3.4_

  - [x] 3.5 Allow bootstrap tokens for 2FA setup endpoint
    - Modify `src/tenxyte/views/twofa_views.py` - TwoFactorSetupView.post() at line 110
    - Update @require_jwt decorator usage to accept tokens with scope="2fa_setup_only" in addition to full-scope tokens
    - Verify endpoint only accepts bootstrap tokens or full-scope tokens, reject invalid scopes
    - _Bug_Condition: Bootstrap token must access /2fa/setup/ endpoint_
    - _Expected_Behavior: can_call_endpoint(result.access_token, "/2fa/setup/") = true_
    - _Preservation: Full-scope token access unchanged_
    - _Requirements: 2.2_

  - [x] 3.6 Allow bootstrap tokens for 2FA confirmation endpoint
    - Modify `src/tenxyte/views/twofa_views.py` - TwoFactorConfirmView.post() at line 182
    - Update @require_jwt decorator usage to accept tokens with scope="2fa_setup_only" in addition to full-scope tokens
    - After successful 2FA activation (line ~250), generate and return a new full-scope token pair
    - Invalidate the bootstrap token after confirmation (if token blacklisting is implemented)
    - _Bug_Condition: Bootstrap token must access /2fa/confirm/ endpoint_
    - _Expected_Behavior: can_call_endpoint(result.access_token, "/2fa/confirm/") = true AND return full-scope token after confirmation_
    - _Preservation: Full-scope token confirmation flow unchanged_
    - _Requirements: 2.2, 2.3_

  - [x] 3.7 Reject bootstrap tokens for other protected endpoints
    - Identify all other protected endpoints (e.g., TwoFactorDisableView, user management endpoints)
    - Ensure @require_jwt decorator rejects tokens with scope="2fa_setup_only"
    - Return 403 INSUFFICIENT_SCOPE for bootstrap tokens attempting to access restricted endpoints
    - _Bug_Condition: Bootstrap token must NOT access endpoints other than /2fa/setup/ and /2fa/confirm/_
    - _Expected_Behavior: can_access(result.token, endpoint) = false for all endpoints except 2FA setup/confirm_
    - _Preservation: Full-scope token access unchanged_
    - _Requirements: 2.4_

  - [x] 3.8 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Super Admin Bootstrap Token Issuance
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify all assertions pass:
      - Admin without 2FA receives restricted token
      - Token scope is "2fa_setup_only"
      - Token lifetime is 15 minutes (900 seconds)
      - requires_2fa_setup flag is true
      - Token can access /2fa/setup/ and /2fa/confirm/
      - Token cannot access other endpoints
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_

  - [x] 3.9 Verify preservation tests still pass
    - **Property 2: Preservation** - Existing Authentication Flows Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix:
      - Regular users can log in without 2FA requirements
      - Admin with 2FA enabled must provide TOTP code
      - Token refresh validates admin 2FA status
      - 2FA disable remains restricted
      - Backup code authentication unchanged
      - Magic link and social auth flows unchanged
      - Security features (session limits, device fingerprinting) unchanged
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6_

- [-] 4. Checkpoint - Ensure all tests pass
  - Run complete test suite including bug condition and preservation tests
  - Verify no regressions in existing authentication flows
  - Test edge cases:
    - Bootstrap token expiration (attempt to use after 15 minutes)
    - Bootstrap token used for unauthorized endpoint (should get 403 INSUFFICIENT_SCOPE)
    - Restricted tokens cannot be refreshed
  - Run integration test for full bootstrap flow:
    - Create super admin via createsuperuser
    - Log in and receive restricted token
    - Call /2fa/setup/ with restricted token
    - Call /2fa/confirm/ with valid TOTP code
    - Receive full-scope token
    - Access protected endpoints successfully
  - Verify subsequent logins require TOTP code (no more bootstrap tokens)
  - Ask the user if questions arise
